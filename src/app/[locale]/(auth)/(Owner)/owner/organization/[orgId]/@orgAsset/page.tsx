import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function OrgAsset() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Total assets</CardTitle>
      </CardHeader>
      <CardContent>Is not a valid asset</CardContent>
    </Card>
  )
}
